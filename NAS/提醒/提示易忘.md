
# 更新包与安装包
.ins 是安装包
.bz2 是更新包

# 删除元数据
test@TNAS:/# dd if=/dev/zero of=/dev/sdd bs=1M count=1000  
1000+0 records in  
1000+0 records out  
1048576000 bytes (1.0 GB, 1000 MiB) copied, 2.60611 s, 402 MB/s

# 删除HyperLock卷

df -h
	找到输出中的：/dev/mapper/vg2-lv5   10G  105M  9.9G   2% /Volume6
umount /Volume6   （删除的是HyperLock对应的卷）
lvremove vg2/lv5
	Do you really want to remove and DISCARD active logical volume vg2/lv5? [y/n]: y
	  Logical volume "lv5" successfully removed

# 【USB 设备】页通常只显示**外接盘模式**（可挂载/可格式化/可加密）的设备。
代码校验
findmnt | egrep '/dev/sdg|/dev/sdk|/dev/sdi' || echo "OK：这三块盘当前没有挂载，所以 USB 设备页会显示为空"  # 没输出=没挂载

cat /proc/mdstat | egrep 'sdg|sdk|sdi' || echo "OK：未发现这三块盘在 mdraid 阵列中" # 如果出现 sdg/sdi/sdk 说明被阵列占用，USB页不会显示 pvs 2>/dev/null | egrep 'sdg|sdk|sdi' || echo "OK：未发现这三块盘被 LVM 作为 PV 使用" # 如果出现，说明被存储池/卷占用，USB页不会显示

findmnt | egrep '/dev/md1|md1' || echo "OK: md1 当前未挂载"  


# 证书过期
TOS 桌面 → 控制面板 → 安全 → 证书（Certificates）->点击刷新


# 查询新设备的ip
方法一：
	连接外接显示屏，登录账号，使用语句 ip a
方法二：
	查看mac地址，在TNAS PC应用中找到对应mac地址的设备

# 更新低版本系统指令
vim /usr/lib/version 
i
esc
:wq!


# 查看缓存配置信息
cat /etc/lvmcache/cache.conf

![[企业微信截图_17733068574375.png]]


# 只有配置界面才需要字体置灰
挂载界面字体不灰色正常的


# 当前非系统盘

![[Pasted image 20260305182904.png]]

![[Pasted image 20260305182921.png]]

# 配置中
将另个NAS的阵列的磁盘放另一台NAS的DAS中，会出现配置中
![[Pasted image 20260308180611.png]]